#!/usr/bin/env bash

menu() {
    echo -e $'\n\n\n\n' | rofi -theme ~/.config/rofi/powermenu.rasi -dmenu -i -p "Power Menu -dpi 0"
}

choice="$(menu)"

case $choice in 
    "")
        systemctl poweroff
        ;;
    "")
        systemctl reboot
        ;;
    "")
        betterlockscreen -l
        ;;
    "")
        systemctl suspend
        ;;
    "")
        qtile cmd-obj -o cmd -f shutdown
        ;;
    *)
        ;;
esac
